name: campus_connect
description: A new Flutter project for Campus Connect.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Updated for Dart 3 compatibility

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8 # Updated to latest
  supabase_flutter: ^2.6.0 # Added based on your project goals

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0 # Added for modern best practices

flutter:
  uses-material-design: true

  assets:
    - assets/images/Novara logo.jpeg
    - assets/images/Makerere University Logo PNG Vector (PDF) Free Download.jpeg
